# Testing

Filled in from M0 onward. Target coverage: template selection, config validation, editor state, preview and public rendering, missing optional content, invalid content, media failures, routing, API validation, persistence, authorization.
Critical journey with mandatory e2e coverage: select template -> customize -> preview -> save -> publish -> open public URL.
